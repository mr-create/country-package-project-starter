# Fictional governed sources

The DOCX files in this directory are fictional exercise evidence. Their country-code filename prefix is used as the POC authorization metadata. The Evidence Copilot reads only sources matching the current Editor's authorized package country.

The adjacent text files make the fictional source content reviewable without specialized tooling; the DOCX versions model the unstructured repository described by the assignment.
